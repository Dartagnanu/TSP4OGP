import networkx as nx


def find_shortest_path(store, upcs, start, end):
    #TODO: Fetch the graph from the database
    #G = fetch_graph_from_db(store)
    
    #TODO: fetch all the item indexes from the database
    #list item_locations; 
    #for upc in upcs:
    #    item_locations.append(fetch_item_location_from_db(upc))
    

    #find shortest cycle that visits all upcs
    pass